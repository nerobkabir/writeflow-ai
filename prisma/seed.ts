/**
 * Full database seed is implemented in prisma/seed.js (run via `npx prisma db seed`).
 *
 * Default credentials after seed:
 *   user@writeflow.com / 123456  (id: user-phase1)
 *   admin@writeflow.com / 123456 (id: admin-phase1)
 *
 * Passwords are stored bcrypt-hashed.
 */

export {};
